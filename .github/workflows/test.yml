name: Test

on:
  push:
    branches: [ $default-branch ]
  pull_request:
    branches: [ $default-branch ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo $ {{ secrets.MATE }}
          echo ${{ secrets.TEST }}
          echo ${{ secrets.EMAIL_USER }}
          echo ${{ secrets.NEPOSTOJI }}
          echo "Hello"
